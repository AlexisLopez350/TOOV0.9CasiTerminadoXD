@model IEnumerable<blankspaces.Models.SUBCATEGORIA>

@{
    ViewBag.Title = "Index";
}

<h2>Gestionar SubCategoria</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            SubCategoria
        </th>
        <th>
            Categoria
        </th>
        <th>
            Descripcion
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
           @Html.DisplayFor(modelItem => item.IDSUBCATEGORIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CATERGORIA.IDCATEGORIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IDSUBCATEGORIA }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IDSUBCATEGORIA }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IDSUBCATEGORIA })
        </td>
    </tr>
}

</table>
